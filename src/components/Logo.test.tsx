export {}

/**
 * @jest-environment jsdom
 */

/*
import React from 'react'
import { render } from '@testing-library/react'
import Logo from './Logo'

test('renders logo', () => {
  render(<Logo />)
  const logoText = screen.getByText(/wolt/i)
  // expect(linkElement).toBeInTheDocument()
  expect(1).toBe(1)
})
*/
